# Experiments

This directory is for reproducible benchmark runs and report drafts.

Suggested workflow:

1. Define benchmark config in `benchmark_template.yaml`.
2. Run experiments with fixed seeds and explicit hardware metadata.
3. Summarize into `report_template.md`.
